name: jupyter

resources:
  instance_type: g6.xlarge

setup: |
  pip install --upgrade pip
  conda init bash
  conda create -n jupyter python=3.12 -y
  conda activate jupyter
  pip install jupyter

run: |
  conda activate jupyter
  nohup jupyter lab &> ./jupyter.log &
  tail -f +1 jupyter.log